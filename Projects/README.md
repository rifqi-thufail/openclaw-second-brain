# Projects

Active work with a defined outcome and deadline. Each project gets its own note (use the project template).

Rules:
- A project has an end date. When done, move it to Archives/
- If it has no end date, it is an Area, not a Project
